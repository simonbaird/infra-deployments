resources:
# Later we might want to pin this to a version, or download it and split it into
# its separate pieces, but for now let's just use the official release directly
- https://storage.googleapis.com/tekton-releases/chains/latest/release.yaml

patchesStrategicMerge:
# Add some chains configuration
- chains-config.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
